{
  "name": "motek",
  "version": "2.0.0-alpha.38",
  "private": true,
  "description": "Aplikacja do dopasowywania wzorów dziewiarskich do zapasu włóczek.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test --test-isolation=none",
    "check": "node --check server.js && node --check material-policy.js && node --check matching-policy.js && node --check client-policy.js && node --check theme-policy.js && node --check app.js && npm test",
    "test:db": "supabase start && supabase db reset --local && supabase test db --local",
    "patterns:check": "node --env-file-if-exists=.env scripts/import-patterns.js",
    "patterns:import": "node --env-file-if-exists=.env scripts/import-patterns.js --execute",
    "staging:check": "node --test test/staging-config.test.js test/deployment-policy.test.js test/observability.test.js test/proxy-policy.test.js test/readiness.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.110.8"
  },
  "devDependencies": {
    "jsdom": "30.0.1",
    "supabase": "2.111.0"
  }
}
