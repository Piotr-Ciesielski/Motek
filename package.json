{
  "name": "motek",
  "version": "1.0.2",
  "private": true,
  "description": "Aplikacja do dopasowywania wzorów dziewiarskich do zapasu włóczek.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check app.js && node --test"
  },
  "dependencies": {
    "sql.js": "^1.13.0"
  }
}
